name: Docker CI/CD with AWS Deploy (without tests)

on:
  push:
    branches: ["main"]

jobs:
  build-push-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_KEY }}

      - name: Push image to Docker Hub
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/flask_app:latest

      - name: Deploy to AWS EC2
        if: success()
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.AWS_HOST }}
          username: ${{ secrets.AWS_USERNAME }}
          key: ${{ secrets.AWS_PRIVATE_KEY }}
          port: ${{ secrets.AWS_PORT || 22 }}
          script: |
            echo "🚀 Starting deployment to AWS..."
            
            cd /home/ec2-user/flask-app

            # Update repository files
            git pull origin main

            # Pull latest Docker image from Docker Hub
            sudo docker pull ${{ secrets.DOCKER_HUB_USERNAME }}/flask_app:latest

            # Update docker-compose.yml with the correct image tag
            sudo sed -i 's/image: flask_app:latest/image: ${{ secrets.DOCKER_HUB_USERNAME }}\/flask_app:latest/' docker-compose.yml

            # Clean up old containers and networks before starting new ones
            sudo docker-compose down -v || true

            # Start services with the updated image
            sudo docker-compose up -d --force-recreate

            # Verify services are running
            echo "📋 Running containers:"
            sudo docker-compose ps

            # Prune old images
            sudo docker image prune -f
            
            echo "✅ Full stack deployment completed successfully!"
